<%= form_with(model: morning_slot, local: true) do |form| %>
  <% if morning_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(morning_slot.errors.count, "error") %> prohibited this morning_slot from being saved:</h2>

      <ul>
      <% morning_slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :morning_slot_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
